import {Component} from '@angular/core';

@Component({
  selector: 'returns',
  providers: [],
  styles: [],
  template: `<router-outlet></router-outlet>`
})
export class Returns {

  constructor() {
  }
}
